using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// 농사 자동화 과정에서 여러 클래스가 공통으로 사용하는 정보를 묶어 전달한다.
/// 현재 해골, 농작지, 보관함, 자동화 단계 등의 공유 데이터를 보관한다.
/// </summary>
public class FarmingContext : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
